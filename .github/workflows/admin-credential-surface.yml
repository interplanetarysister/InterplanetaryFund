name: Admin credential surface

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "convex/adminUsers.ts"
      - "convex/auth.ts"
      - "convex/security.ts"
      - "package.json"
      - "scripts/verify-admin-auth-boundary.mjs"
      - "scripts/verify-admin-credential-surface.mjs"
      - ".github/workflows/admin-credential-surface.yml"

permissions:
  contents: read

concurrency:
  group: admin-credential-surface-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact reviewed head
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Setup Node 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify canonical admin authorization boundary
        run: npm run test:admin-auth-boundary

      - name: Verify public admin credential surface
        run: npm run verify:admin-credential-surface

      - name: Typecheck current source
        run: npx tsc --noEmit

      - name: Build production application
        run: npm run build
