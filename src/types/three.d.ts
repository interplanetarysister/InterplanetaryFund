// Three.js is consumed as a runtime module by Globe.tsx and the repository currently
// does not ship @types/three. Keep the declaration intentionally minimal until the
// runtime dependency is migrated to an explicitly typed integration.
declare module "three";
